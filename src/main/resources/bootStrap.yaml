spring:
  application:
    name: k8spipeline
  cloud:
    kubernetes:
      config:
        sources:
          - name: k8spipeline
      reload:
        enabled: true
        monitoring-secrets: true
      secrets:
        enable-api: true
        labels:
          app: k8spipeline