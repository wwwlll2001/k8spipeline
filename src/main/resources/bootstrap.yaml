spring:
  application:
    name: k8spipeline123
  cloud:
    kubernetes:
      reload:
        enabled: true
        monitoring-secrets: true
      secrets:
        enable-api: true
        labels:
          app: k8spipeline